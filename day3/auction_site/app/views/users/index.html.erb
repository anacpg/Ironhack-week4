
<h2>Users</h2>
<ul>
  <% @users.each do |user| %>
    <li>  <%=  link_to user.name, users_show_path(user) %>
          <%= link_to 'Destroy', users_destroy_path(user)%>
    </li>

  <% end %>
</ul>

<%= link_to 'New User', users_new_path %>
